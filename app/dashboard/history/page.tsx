import { recentChecks } from '@/lib/mock-data'
import { Badge } from '@/components/ui'
export default function History(){return <div><p className="text-sm text-[var(--muted)]">Workspace / History</p><h1 className="mt-1 text-3xl font-semibold">Check history</h1><div className="mt-8 rounded-xl border border-[var(--line)] bg-white p-6"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-[var(--line)] text-xs text-[var(--muted)]"><tr><th className="pb-3">Check</th><th className="pb-3">Identifier</th><th className="pb-3">Result</th><th className="pb-3 text-right">Time</th></tr></thead><tbody>{recentChecks.concat(recentChecks).map((c,i)=><tr key={i} className="border-b border-[var(--line)] last:border-0"><td className="py-4 font-medium">{c.service}<div className="text-xs font-normal text-[var(--muted)]">{c.id}</div></td><td className="py-4 font-mono text-xs">{c.identifier}</td><td className="py-4"><Badge className={c.tone}>{c.result}</Badge></td><td className="py-4 text-right text-xs text-[var(--muted)]">{c.time}</td></tr>)}</tbody></table></div></div></div>}
